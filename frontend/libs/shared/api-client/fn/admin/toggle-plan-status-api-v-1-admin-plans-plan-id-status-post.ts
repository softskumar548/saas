/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SubscriptionPlan } from '../../models/subscription-plan';

export interface TogglePlanStatusApiV1AdminPlansPlanIdStatusPost$Params {
  plan_id: number;
  is_active: boolean;
}

export function togglePlanStatusApiV1AdminPlansPlanIdStatusPost(http: HttpClient, rootUrl: string, params: TogglePlanStatusApiV1AdminPlansPlanIdStatusPost$Params, context?: HttpContext): Observable<StrictHttpResponse<SubscriptionPlan>> {
  const rb = new RequestBuilder(rootUrl, togglePlanStatusApiV1AdminPlansPlanIdStatusPost.PATH, 'post');
  if (params) {
    rb.path('plan_id', params.plan_id, {});
    rb.query('is_active', params.is_active, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SubscriptionPlan>;
    })
  );
}

togglePlanStatusApiV1AdminPlansPlanIdStatusPost.PATH = '/api/v1/admin/plans/{plan_id}/status';
